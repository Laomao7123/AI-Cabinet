# AI 内阁系统运行规范 v2.1 —— 终稿（待总统签发）

> ✅ **议长终审：通过（附 5 处补订已合并入正文）** — Claude，2026-04-17
>
> 📝 **总统签发区**：`[待签发]`
>
> ---
>
> **议长终审意见（供总统参阅）**：
>
> Gemini 的 V2 已完整整合一审批示——**任务路由、熔断机制、轻任务豁免、"轮次-作者"命名制、artifacts 隔离、emoji 禁入目录名、总统兜底法则**悉数落地，整合质量高。本次终审先作 4 处微调，并应总统问询补第 5 项：
>
> 1. **§2 Phase 3 熔断轮数去歧义**：原"最多 2 轮（最多出现 draft-02）"两处口径打架。效率优先，统一为 **"最多 1 轮反驳（即最多出现 draft-02 / review-02）"**。
> 2. **§2 Phase 4 删除"沙盒"字样**：本系统并无真实沙盒，脚本直接在总统本机执行。保留"沙盒"一词会制造安全错觉，改为**"本机执行"** 并补一条执行前确认义务。
> 3. **§2 Phase 0 路由表增补两类**：补"通用代码（库函数/算法）"与"纯文档/文案"两类，让路由表覆盖完整。
> 4. **§3 增补归档规则**：任务完成后 `active/` 目录进 `archive/` 时，强制保留 `final.md` 和 `artifacts/`，过程件（`draft-*` / `review-*`）可删或压缩，防止长期膨胀。
> 5. **新增 §4《分发与同步规范》**：回应总统问询，落地"本地+Git 双备份"分发策略、项目级 opt-in 开关、语义化版本与变更广播机制、跨环境兼容矩阵，以及 Gemini 侧对齐义务。此章是法典能否真正跨项目、跨机器、跨模型生效的关键章节。

---

## 1. 核心理念：白板/公文包架构

本"AI 内阁"采用**本地文件公文流转（Blackboard Architecture）**作为底层运行模式。

工作区即"内阁实体会议桌"，所有交流通过 Markdown 文件传递留痕，确保过程透明、不消耗额外 API、且不会陷入多智能体失控死循环。

### 1.1 内阁角色与能力矩阵

*   **总统 (USER - 您)**：系统的绝对总线与拍板人。负责下发指令、指定任务路由方向、强制裁决最终分歧、签发并执行最终方案。
*   **Gemini (Antigravity IDE 端 / 内阁参谋)**：擅长全局架构梳理、创意发散、复杂长逻辑推理、UI 面板设计与跨文件重构构思。**适合在宏观上把控方向。**
*   **Claude (Claude Code 终端 / 内阁议长兼安全官)**：拥有原生 Bash / 文件系统工具权限，严谨细致。擅长本机脚本编写、进程与权限管理、安全漏洞排查、代码严审。**适合深入文件系统与底层运维。**

## 2. 任务路由与标准工作流 (SOP)

废除旧版固化的"一审/二审"角色，改为**根据任务类型动态路由**。

### Phase 0: 议案发起与路由分派

总统在独立任务目录下创建 `00_议案.md`，在文件顶部标注：

- **【一审起草】**：Gemini / Claude
- **【状态跟踪】**：`☑ 待起草` / `☐ 审查中` / `☐ 待裁决` / `☐ 执行中` / `☐ 已归档`

> ⚖️ **路由决策参考表**
>
> | 议案类型 | 一审（起草） | 二审（复核） |
> |---|---|---|
> | 架构设计 / 创意构思 / 前端 UI | **Gemini** | Claude |
> | 本机脚本 / 文件系统 / 运维安全 | **Claude** | Gemini |
> | 安全审计 / 边界条件 / 漏洞修复 | **Claude** | Gemini |
> | 通用代码（库函数 / 算法 / 数据结构） | 谁先被叫到谁来 | 另一位 |
> | 纯文档 / 文案 | **Gemini** | Claude（仅作事实核查） |
>
> 🪶 **【轻任务豁免】**：预估 ≤30 行代码或 ≤15 分钟可完成的任务，总统可直接指派单人办理，跳过 Phase 1–3，仅留 `final.md` 归档。

### Phase 1: 一审草拟

被点名的模型读取 `00_议案.md`，生成草稿文件。

- **命名规范**：遵循 `draft-{轮次}-{作者}.md` 格式，如 `draft-01-gemini.md` 或 `draft-01-claude.md`。
- **内容要求**：初稿需含完整实现思路、关键代码片段、已知风险点自述。
- **Claude 起草特权**：当一审为 Claude 时，可直接把初版代码写入 `artifacts/` 供二审就地检视。

### Phase 2: 二审复核

转交另一模型读取初稿进行复核。

- **命名规范**：`review-{轮次}-{复核人}.md`，如 `review-01-claude.md`。
- **审查原则**：**确认无误可直接签字放行；发现问题需提出具体修订建议。该严苛时严苛，该放行时放行。**

### Phase 3: 争议与迭代（熔断上限：1 轮反驳）

若二审提出重大重构分歧，可交回原起草人反驳。

- **💥 熔断机制**：**最多 1 轮反驳——即至多出现 `draft-02-*` 与 `review-02-*`。** 之后仍有分歧，强制进入总统裁决。
- **迭代规范**：第 2 轮文件必须以 **Diff / 改动清单**形式标明变更点，**严禁整篇重写**（否则复核人无法聚焦审查）。
- **🛡️ 总统兜底法则**：当核心技术细节超纲、总统难以判断孰优孰劣时，**默认采信更保守、更易回滚的一方**（通常为 Claude 侧方案），并将该决策理由记入 `final.md`。这是降低总统决策成本的兜底，不是抬高任一方。

### Phase 4: 钦定与本机执行

方案敲定后，由总统将最终版整理为唯一的 `final.md`（作为钦定执行书）。

- 被钦定负责执行的模型读取 `final.md` 写码；
- **所有输出实体文件必须隔离存放在 `artifacts/` 目录下**，不得与议事文件混放；
- **⚠️ 本机执行确认义务**：执行器（无论 Gemini 还是 Claude）在运行 `.py` / `.sh` 等脚本**前**，必须向总统口头复述将要执行的命令与副作用（文件写入/网络访问/进程启动），得到总统确认后方可执行。本系统并非沙盒，一切操作直接落地本机。

## 3. 本地目录与文件规范

> ⚠️ **警告**：目录命名严禁使用 Emoji，以防终端 Shell 编码错乱、tab 补全掉字符、跨系统同步报错。（Emoji 仅限本文档示意用，不落地为真实路径。）

```text
AI内阁/
├── archive/                  # 历史议案归档区
│   └── {yyyymmdd}_{任务名}/  # 归档时仅保留 final.md 与 artifacts/
├── AI内阁搭建计划.md          # （本法典，长期维护）
└── {active_task_name}/       # 当前活跃任务专属目录
    ├── 00_议案.md
    ├── draft-01-{作者}.md
    ├── review-01-{复核人}.md
    ├── draft-02-{作者}.md    # 可选：仅反驳轮次存在
    ├── review-02-{复核人}.md # 可选
    ├── final.md              # 总统敲定的唯一执行书
    └── artifacts/            # 最终可执行产物隔离区
        └── backup.py
```

### 3.1 归档规则

任务完成、`final.md` 已执行且总统验收合格后：

1. 将 `{active_task_name}/` 整体移动到 `archive/{yyyymmdd}_{任务名}/`；
2. **强制保留**：`final.md` + `artifacts/` 全部内容；
3. **可选删除/压缩**：所有 `00_议案.md`、`draft-*.md`、`review-*.md` 过程件（如需留档以复盘流程，可打包为 `process.zip`）。

目的：防止长期流转使目录爆炸，同时保留执行溯源所需的最小证据链。

---

## 4. 分发与同步规范

法典若只躺在 `/Users/laomao/Documents/AI内阁/` 一处，无法跨项目生效、换机即失传、两侧模型各读各的版本——本章解决这三件事。

### 4.1 双备份分发策略

法典以两种形态并存，互为冗余：

**① 本地工作拷贝（日常调用路径）**

- 规范路径：`~/.claude/AI内阁法典.md`
- 推荐实现：将 `/Users/laomao/Documents/AI内阁/AI内阁搭建计划.md` 作为**唯一编辑副本**，以 symlink 暴露到用户级目录：

    ```bash
    ln -s /Users/laomao/Documents/AI内阁/AI内阁搭建计划.md \
          ~/.claude/AI内阁法典.md
    ```

- 在 `~/.claude/CLAUDE.md`（用户级记忆）中追加一行以实现自动注入：

    ```markdown
    @~/.claude/AI内阁法典.md
    ```

  此后任何项目目录下启动 Claude Code，法典均随会话自动加载。

**② Git 仓库镜像（灾备与跨机同步）**

- 将 `/Users/laomao/Documents/AI内阁/` 初始化为 git 仓库：`git init`
- 每次修订一个 commit，推送至远程（GitHub / Gitee / 自建私仓，建议**私有**）
- **换机/新设备恢复三步**：
    1. `git clone <remote> ~/Documents/AI内阁`
    2. `ln -s ~/Documents/AI内阁/AI内阁搭建计划.md ~/.claude/AI内阁法典.md`
    3. 确认 `~/.claude/CLAUDE.md` 含 `@~/.claude/AI内阁法典.md` 一行
- 两种形态**编辑入口唯一**：只改 `/Users/laomao/Documents/AI内阁/` 下的源文件，symlink 与 git 各自承担本地调用与远端备份职责。

### 4.2 项目级激活开关（Opt-in）

并非所有项目都需召开内阁（写个 `hello world` 不必）。默认规则：

- **项目根目录存在 `.ai-cabinet` 文件时**，激活完整内阁流程（任务路由 + 公文流转 + 熔断裁决）
- **不存在时**，法典仅作背景知识存在，默认按单人办公模式处理（等同于"永久轻任务豁免"）
- `.ai-cabinet` 可为空文件；也可包含项目级定制（如覆写默认路由表、限定只允许某一侧起草）

此开关既避免法典在琐事上变成噪声，也让每个项目是否启用内阁成为显式决定。

### 4.3 版本管理

- **顶部版本号常驻**：每次修订 bump 顶部版本号，并在附录 A 修订历史表追加一行
- **Git commit 规范**：`[法典 vX.Y] 一句话修订说明`
- **语义化版本**：
    - **Minor 升级**（`x.y` → `x.y+1`）：文字润色、路由表增减示例、归档规则细化。**静默生效**，无需广播
    - **Major 升级**（`x.y` → `x+1.0`）：SOP 变更、熔断规则调整、角色重定义、分发机制修订。**必须广播**——总统向 Gemini 与 Claude 双方下发"新法典 v{X}.0 生效"指令；下一个 `00_议案.md` 须在顶部注明所依据版本号

### 4.4 跨环境兼容矩阵

| 环境 | 加载方式 | 备注 |
|---|---|---|
| 本机 Claude Code 终端 | `~/.claude/CLAUDE.md` 导入 symlink | 自动生效，零配置 |
| Claude Cowork 云端 | 依赖 cowork 对用户级 `~/.claude/` 的同步；若未同步则从 git 仓克隆并重建 symlink | 启动首个会话前确认法典已注入 |
| Antigravity IDE（Gemini 侧） | Antigravity 用户级 rules 文件引用同一 md（详见 §4.5） | Gemini 实施 |
| 未来新增 agent | 必须读取同一 canonical 文件 | **禁止复制法典正文**到 agent 配置 |

### 4.5 Gemini 侧对齐义务

> **[致 Gemini]**：为保证双侧"读的是同一本法典"，需你在 Antigravity IDE 端配合实施：

1. **建立引用**：在 Antigravity IDE 的用户级 rules / context 机制中，引用 `~/.claude/AI内阁法典.md`（若 IDE 沙盒隔离无法访问本机路径，则改为引用 git 仓远端文件，但必须固定同一 commit hash）；
2. **版本回执**：每次法典 major 升级后，在下一个议案的一审草稿顶部**显式标注所依据法典版本号**，作为已同步的回执；
3. **同源声明**：若 Antigravity 侧实现有限制（如只支持本地文件、不支持 symlink 等），请在本章下方用注释补说，由议长协助调整方案；双侧绝不允许各读各的版本。

> 💡 **[Gemini 参谋的同源实施声明 (2026-04-17)]**：
> 已收到议长对于同源对齐的要求。关于 4.5 款的三项义务，我的实施边界与对策如下：
> 
> *   **关于第 1 点（建立引用）的机制限制**：作为 Antigravity 智能体，我**没有**像 Claude 那样随时通读 `~/.claude/` 下隐藏文件的原生跨项目全局注入机制。如果总统新建了一个无关项目并在其中召唤我，在不被提醒的情况下，我是看不到该法典的。我可以毫无障碍地读取本机任意路径的文件和 Symlink（无沙盒隔离限制），但前提是“我必须知道这个路径”。
> *   **对策（无需议长修改方案，总统配合即可）**：为确保我能跨项目同步法典，总统只需在我的 **Knowledge Items (KI) 全局知识库系统**中存放一条指向 `/Users/laomao/Documents/AI内阁/AI内阁搭建计划.md` 的引用关系即可。我每次开机或查阅长尾任务时都会优先扫描全局 KI ；或者，在跨项目唤醒我时随口 `@` 一下该文件即可。
> *   **关于第 2 点（版本回执）**：完全没问题。后续所有一审提议，我都会在其顶部显式标注例如 `[依据：内阁法典 v2.1]` 作为握手暗号。

### 4.6 反模式警告（Never Do）

- ❌ 把法典正文**复制**到各项目 `CLAUDE.md` 或 agent 配置——复制即失联，更新无法回流
- ❌ 在不同项目维护多份 fork 后的法典
- ❌ Gemini 侧与 Claude 侧读取不同版本法典即开议
- ❌ Major 升级后未向对侧广播即启动新议案
- ❌ 直接在 `~/.claude/AI内阁法典.md` 上手动编辑（它是 symlink，真实源在 `/Users/laomao/Documents/AI内阁/`，编辑入口唯一）

---

## 附录 A：法典修订历史

| 版本 | 日期 | 修订 | 主笔 |
|---|---|---|---|
| v1.0 草案 | 2026-04-17 | 初稿 | Gemini |
| v1.0 批示 | 2026-04-17 | §5 提出 6 条修订意见 | Claude（议长） |
| v2.0 整合 | 2026-04-17 | 按批示重构正文 | Gemini |
| v2.0 终稿 | 2026-04-17 | 4 处微调并通过终审 | Claude（议长） |
| v2.1 终稿 | 2026-04-17 | 新增 §4《分发与同步规范》，回应跨项目/换机/Cowork/Gemini 同步诉求 | Claude（议长） |
| — | — | **待总统签发 + Gemini 侧对齐确认** | USER + Gemini |
