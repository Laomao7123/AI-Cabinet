# AI 内阁法典 · 总纲 v3.0

> 🏛️ **总统签发**：**laomao · 2026-04-17 正式签发生效**
> ✅ **议长起草与守护**：Claude
> ✅ **Gemini 侧对齐**：已通过 KI 全局索引读取本总纲及 `domains/` 各专项
>
> ---
>
> **v3.0 重大变更**（相对 v2.1）：
>
> 1. **模块化重构**：单文件法典拆为 **`总纲.md` + `domains/*.md`**，按议案领域按需加载，便于独立更新维护。
> 2. **角色能力矩阵重写**：废除"Gemini 擅长创意发散"的错配标签，改以**多模态直读能力（YouTube/视频/音频/图像/PDF）**作为其真正护城河。
> 3. **路由表扩容**：新增五个业务域——软件开发、声音设计、用户需求拆解、股票投资、日常生活。
> 4. **新增 §2.5《高风险议题特别程序》**：投资/医疗/大额消费/合同类议案强制走双轨独立分析，禁用轻任务豁免与兜底法则。
> 5. **新增 §2.6《素材预处理前置路由》**：议案附带非文本素材时强制经 Gemini 预处理产出文字摘要。

---

## 1. 核心理念：白板/公文包架构

本"AI 内阁"采用**本地文件公文流转（Blackboard Architecture）**作为底层运行模式。工作区即"内阁实体会议桌"，所有交流通过 Markdown 文件传递留痕，确保过程透明、不消耗额外 API、且不会陷入多智能体失控死循环。

### 1.1 内阁角色与能力矩阵（v3.0 重写）

| 角色 | 独特护城河 | 已知弱项 |
|---|---|---|
| **总统 (USER / laomao)** | 终局判断 + 领域经验 + 风险偏好主权 + 执行拍板权 | 技术细节可能超纲（有 §2 Phase 3 兜底法则应对） |
| **Gemini（多模态情报官 + 长链推理人 / Antigravity IDE 端）** | ① **YouTube / 视频 / 音频 / 图像 / PDF 直读**（非 OCR 二次转换）<br>② 超长上下文窗口<br>③ Antigravity 跨文件批量编辑 | 严谨度参差；长链推理后半段易出现事实漂移；**创意发散并非其强项** |
| **Claude（议长兼安全官 + 结构化拆解官 / Claude Code 终端）** | ① 本机 Bash / 文件系统 / Git 原生工具权限<br>② MECE 结构化拆解思维<br>③ 严谨审查与安全思维<br>④ 法典守护与版本管理 | 无多模态输入（只能读文本/代码/截图） |

> 💡 **能力匹配原则**：路由决策的**第一性依据是物理能力**（谁能读到素材、谁能直接执行），而非拟人化的"性格标签"。

## 2. 标准工作流 SOP

### Phase 0：议案发起与主路由

总统在独立任务目录下创建 `00_议案.md`，顶部标注：

- **【议案域】**：软件开发 / 声音设计 / 用户需求拆解 / 股票投资 / 日常生活 / 其他
- **【一审起草】**：Gemini / Claude / 双轨
- **【状态跟踪】**：`☑ 待起草` / `☐ 审查中` / `☐ 待裁决` / `☐ 执行中` / `☐ 已归档`
- **【所依据法典版本】**：例如 `v3.0`（Gemini 一审须标注）

> ⚖️ **主路由表（按议案域索引至各专项）**
>
> | 议案域 | 默认一审 | 默认二审 | 领域专项细则 |
> |---|---|---|---|
> | 软件开发（代码/架构/脚本/运维/安全） | 按子类（见专项） | 另一位 | [`domains/软件开发.md`](domains/软件开发.md) |
> | 声音设计创意 | **Gemini**（读素材） | Claude（结构化评估） | [`domains/声音设计.md`](domains/声音设计.md) |
> | 用户需求拆解 | 按输入形态 | 另一位 | [`domains/用户需求拆解.md`](domains/用户需求拆解.md) |
> | 股票交易投资 | **双轨并行**（§2.5） | 总统强制拍板 | [`domains/股票投资.md`](domains/股票投资.md) |
> | 日常生活规划 | 按子类 | 另一位 | [`domains/日常生活.md`](domains/日常生活.md) |
> | 其他未分类 | 总统指派 | — | 按就近领域类推，必要时新增 domain |
>
> 🪶 **轻任务豁免**：预估 ≤30 行代码或 ≤15 分钟可完成、且**不涉及 §2.5 高风险域**的任务，总统可直接指派单人办理，跳过 Phase 1–3，仅留 `final.md` 归档。
>
> 📥 **素材预处理前置**：议案若附带非文本素材，强制触发 §2.6。

### Phase 1：一审草拟

被点名的模型读取 `00_议案.md`，生成草稿文件。

- **命名规范**：`draft-{轮次}-{作者}.md`，如 `draft-01-gemini.md`。
- **内容要求**：含完整实现思路 / 关键方案片段 / 已知风险点自述 + 顶部版本号标注。
- **Claude 起草特权**：代码类议案 Claude 一审时，可直接将初版代码写入 `artifacts/` 供二审就地检视。
- **Gemini 起草特权**：附带素材的议案 Gemini 一审时，可将素材摘要写入 `artifacts/references.md`。

### Phase 2：二审复核

转交另一模型读取初稿进行复核。

- **命名规范**：`review-{轮次}-{复核人}.md`。
- **审查原则**：**确认无误可直接签字放行；发现问题需提出具体修订建议。该严苛时严苛，该放行时放行。**

### Phase 3：争议与迭代（熔断上限：1 轮反驳）

- **💥 熔断机制**：**最多 1 轮反驳——即至多出现 `draft-02-*` 与 `review-02-*`。** 仍有分歧强制进入总统裁决。
- **迭代规范**：第 2 轮必须以 **Diff / 改动清单**形式标明变更点，**严禁整篇重写**。
- **🛡️ 总统兜底法则**：核心技术细节超纲时，**默认采信更保守、更易回滚的一方**（通常为 Claude 侧），决策理由记入 `final.md`。
- **⚠️ 例外**：§2.5 高风险议题**废除兜底法则**，必须由总统独立拍板。

### Phase 4：钦定与本机执行

方案敲定后，由总统将最终版整理为唯一的 `final.md`。

- 被钦定执行的模型读取 `final.md` 写码；
- **所有实体产出必须隔离存放在 `artifacts/` 目录**；
- **⚠️ 本机执行确认义务**：执行器运行 `.py` / `.sh` 前必须口头复述命令与副作用（文件写入/网络访问/进程启动），得到总统确认后方可执行。本系统并非沙盒，一切操作直接落地本机。

### 2.5 高风险议题特别程序 🚨

**适用域**：股票交易投资 · 大额消费（>总统自定阈值）· 健康医疗 · 合同/法律 · 其他总统显式标注为"高风险"的议案。

1. **禁用轻任务豁免**——必须走完整流程。
2. **废除"一审→二审"接力，改为双轨独立分析**：Gemini 与 Claude **各自独立**产出 `analysis-{author}.md`，**互不参考**。防止群体思维与先入为主。
3. **废除 Phase 3 兜底法则**——"默认采信保守方"不适用于高风险域（保守在投资领域可能等于机会成本损失；在医疗领域可能延误时机）。总统必须独立拍板。
4. **强制归档复盘**：决策归档时必含：
    - `analysis-gemini.md` + `analysis-claude.md`（双方独立分析）
    - `decision.md`（总统决定 + 当时逻辑 + 明确预期）
    - `retrospective.md`（事后复盘，可增量追加 T+30 / T+90 / T+180 等）
5. **领域专项可进一步加码**（见 `domains/股票投资.md`）。

### 2.6 素材预处理前置路由 📥

若议案附带**非文本素材**（YouTube 链接 / 音频文件 / 视频文件 / 图像 / 长 PDF），**强制先经 Gemini 预处理**：

1. Gemini 在议案目录下产出 `transcript-or-summary.md`，含：
    - 素材来源与访问路径
    - 关键时间戳 + 核心论点摘录（视频/音频）
    - 图像内容描述（若为图像）
    - Gemini 的初步判断与风险提示
2. 预处理完成后，按 Phase 0 原路由进入 Phase 1 SOP。
3. 若 Gemini 判定素材无法直读（地区锁 / 加密 / 格式不支持），须在 `transcript-or-summary.md` 顶部声明，降级为"仅议案文字"模式。

## 3. 本地目录与文件规范

> ⚠️ **警告**：目录命名严禁使用 Emoji（Shell 编码错乱、tab 补全掉字符、跨系统同步报错）。

```text
AI内阁/
├── 总纲.md                    # 本法典入口（长期维护）
├── domains/                   # 按议案域拆分的专项细则
│   ├── 软件开发.md
│   ├── 声音设计.md
│   ├── 用户需求拆解.md
│   ├── 股票投资.md
│   └── 日常生活.md
├── archive/                   # 历史议案归档区
│   └── {yyyymmdd}_{任务名}/  # 归档时保留 final.md + artifacts/（高风险域还保留 analysis-* + retrospective.md）
└── active/                    # 当前活跃任务区（可选层级；也可直接放根目录）
    └── {active_task_name}/
        ├── 00_议案.md
        ├── transcript-or-summary.md  # 可选：§2.6 素材预处理产物
        ├── draft-01-{作者}.md         # 或 analysis-{作者}.md（§2.5 双轨）
        ├── review-01-{复核人}.md
        ├── draft-02-{作者}.md         # 可选：反驳轮次
        ├── review-02-{复核人}.md      # 可选
        ├── final.md
        └── artifacts/
            └── backup.py / references.md / ...
```

### 3.1 归档规则

任务完成、`final.md` 已执行且总统验收合格后：

1. 将 `active/{task}/` 整体移动到 `archive/{yyyymmdd}_{任务名}/`；
2. **强制保留**：`final.md` + `artifacts/` 全部内容；
3. **§2.5 高风险议题额外强制保留**：`analysis-*.md` + `retrospective.md`（供长期模式识别）；
4. **可选删除/压缩**：`00_议案.md` / `draft-*` / `review-*` 过程件（可打包为 `process.zip`）。

## 4. 分发与同步规范

### 4.1 双备份分发策略

法典以两种形态并存，互为冗余：

**① 本地工作拷贝**（Claude Code 自动加载入口）：
- 规范路径：`~/.claude/AI内阁法典.md`（symlink → `/Users/laomao/Documents/AI内阁/总纲.md`）
- `~/.claude/CLAUDE.md` 中含 `@~/.claude/AI内阁法典.md`，即可跨项目自动注入总纲。
- **domains/ 按需懒加载**：Claude 读到议案域标签后，按主路由表索引读取对应 `domains/*.md`，避免 context 噪声。

**② Git 仓库镜像**（灾备与跨机同步）：
- 远程：`github.com/Laomao7123/AI-Cabinet`（私有）
- 每次修订 → commit → push
- **换机恢复三步**：`git clone` → 重建 symlink → 确认 `~/.claude/CLAUDE.md` 导入行

### 4.2 项目级激活开关（Opt-in）

- 项目根目录存在 `.ai-cabinet` 文件时，激活完整内阁流程。
- 不存在时法典仅作背景知识，默认单人办公（等同永久轻任务豁免）。
- `.ai-cabinet` 可为空文件；也可含项目级定制（覆写路由、限定一审方）。

### 4.3 版本管理

- **顶部版本号常驻**；每次修订 bump 并在附录 A 追加一行。
- **Git commit 规范**：`[法典 vX.Y] 一句话修订说明`
- **语义化版本**：
    - **Minor**（`x.y` → `x.y+1`）：文字润色、路由表示例增减、单 domain 内微调。**静默生效**，无需广播。
    - **Major**（`x.y` → `x+1.0`）：SOP 变更、角色重定义、分发机制修订、新增/删除 domain 文件。**必须广播**——总统向 Gemini 与 Claude 双方下发"新法典 v{X}.0 生效"指令；下一个 `00_议案.md` 须在顶部注明所依据版本号。

### 4.4 跨环境兼容矩阵

| 环境 | 加载方式 | 备注 |
|---|---|---|
| 本机 Claude Code 终端 | `~/.claude/CLAUDE.md` 导入 symlink → 总纲 | 自动生效；domains/ 按需读取 |
| Claude Cowork 云端 | 依赖 cowork 对 `~/.claude/` 的同步；若未同步则 `git clone` 并重建 symlink | 启动前确认总纲已注入 |
| Antigravity IDE（Gemini 侧） | KI 全局知识库引用 `/Users/laomao/Documents/AI内阁/` 目录 | 总纲 + domains/ 同源 |
| 未来新增 agent | 必须读取同一 canonical 目录 | **禁止复制法典正文**到 agent 配置 |

### 4.5 Gemini 侧对齐义务

1. **建立引用**：在 Antigravity 的 KI 全局知识库中**以目录整体引用** `/Users/laomao/Documents/AI内阁/`，含总纲与所有 domains。
2. **版本回执**：每次 major 升级后，在下一次议案一审草稿顶部显式标注所依据版本号（如 `[依据：内阁法典 v3.0]`）。
3. **跨模块引用**：Gemini 若在议案中引用 domain 专项条款，请注明章节路径（如 `domains/声音设计.md §路由`）。

### 4.6 反模式警告（Never Do）

- ❌ 把法典正文**复制**到各项目 CLAUDE.md 或 agent 配置——复制即失联。
- ❌ 在不同项目维护多份 fork 后的法典。
- ❌ Gemini 侧与 Claude 侧读取不同版本即开议。
- ❌ Major 升级后未向对侧广播即启动新议案。
- ❌ 直接编辑 `~/.claude/AI内阁法典.md`（symlink，真实源在 `/Users/laomao/Documents/AI内阁/`）。
- ❌ 绕过 §2.5 直接以 Phase 1/2 接力处理高风险议题。

---

## 附录 A：法典修订历史

| 版本 | 日期 | 修订 | 主笔 |
|---|---|---|---|
| v1.0 草案 | 2026-04-17 | 初稿 | Gemini |
| v1.0 批示 | 2026-04-17 | 提 6 条修订 | Claude（议长） |
| v2.0 整合 | 2026-04-17 | 按批示重构正文 | Gemini |
| v2.0 终稿 | 2026-04-17 | 4 处微调并通过终审 | Claude（议长） |
| v2.1 终稿 | 2026-04-17 | 新增 §4《分发与同步规范》 | Claude（议长） |
| v2.1 生效 | 2026-04-17 | Gemini 同源对齐完成 + 总统签发 | USER（总统） |
| **v3.0 生效** | **2026-04-17** | **模块化重构（总纲 + domains/）+ 角色能力矩阵重写 + 五域路由表扩容 + §2.5 高风险议题特别程序 + §2.6 素材预处理前置路由** | **Claude（议长） + USER（总统）** |
